server:
  port: 4080
spring:
  application:
    name: notification-service
  data:
    mongodb:
      uri: mongodb+srv://devoppscapstone:TPXLQ2laAnNrqQA3@cluster0.mhunonh.mongodb.net/notification_stokvel_db?retryWrites=true&w=majority&appName=Cluster0
      database: notificationdb
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    packages:
      trust-all: true
  jms:
    pub-sub-domain: true # Enable topic (pub-sub) mode
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-service/issuer
logging:
  level:
    com.stockfellow: INFO
    org.springframework.jms: DEBUG

  # Artemis Configuration
  # spring.artemis.mode=embedded
  # spring.artemis.embedded.enabled=true
  # spring.artemis.embedded.persistent=false
  # spring.artemis.embedded.data-directory=target/artemis-data
  # spring.artemis.embedded.queues=group.events
  # spring.artemis.embedded.topics=group.events

  # If using external Artemis broker instead:
  # spring.artemis.mode=native
  # spring.artemis.host=localhost
  # spring.artemis.port=61616
  # spring.artemis.user=admin
  # spring.artemis.password=admin
